# Supabase Configuration
# Related: T099, Setup
# Copy this file to .env and fill in your actual values

# Supabase Project URL
# Find in: Supabase Dashboard > Settings > API > Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon/Public Key
# Find in: Supabase Dashboard > Settings > API > Project API keys > anon/public
# This key is safe to use in client-side code
SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Supabase Service Role Key (DO NOT commit to git)
# Only needed for server-side operations or migrations
# Find in: Supabase Dashboard > Settings > API > Project API keys > service_role
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Notes:
# - DO NOT commit .env file to version control
# - Add .env to .gitignore (already configured)
# - Each developer needs their own .env file
# - For production, use environment variables in hosting platform
